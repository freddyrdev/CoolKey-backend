# Proyecto CoolKey - Backend

1. Descargar la carpeta node_modules
```
npm install
```

2. Copiar la carpeta `.env.template` a `.env` y llenar los datos en el archivo `.env`

3. Iniciar docker con la imagen de PostgreSQL
```
docker-compose up -d
```